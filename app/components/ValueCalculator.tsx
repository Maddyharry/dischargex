"use client";
import { useState } from "react";
export function ValueCalculator(){
  const [cases,setCases]=useState(20),[minutes,setMinutes]=useState(3);
  return <section className="mx-auto max-w-6xl px-6 py-12"><div className="rounded-2xl border border-cyan-400/20 bg-cyan-400/5 p-6 sm:p-10"><p className="text-sm text-cyan-300">ประเมินสำหรับงานของคุณ</p><h2 className="mt-2 text-2xl font-semibold">ถ้างานต่อเคสใช้เวลาน้อยลง คุณได้เวลากลับมาเท่าไร</h2><div className="mt-6 grid gap-6 sm:grid-cols-2"><label>จำนวนเคสต่อวัน <input className="mt-2 block w-full rounded border border-slate-600 bg-slate-900 p-3 text-white" type="number" min="1" max="500" value={cases} onChange={e=>setCases(Math.max(1,Math.min(500,Number(e.target.value)||1)))}/></label><label>เวลาหลังใช้ รวมตรวจทาน (นาที/เคส) <input className="mt-2 block w-full rounded border border-slate-600 bg-slate-900 p-3 text-white" type="number" min="0.5" max="60" step="0.5" value={minutes} onChange={e=>setMinutes(Math.max(.5,Math.min(60,Number(e.target.value)||.5)))}/></label></div><p className="mt-6 text-xl">ส่วนต่างประมาณ <strong className="text-cyan-200">{(cases*(5-minutes)/60).toFixed(1)} ถึง {(cases*(10-minutes)/60).toFixed(1)} ชั่วโมง/วัน</strong></p><p className="mt-3 text-sm leading-relaxed text-slate-400">เครื่องคำนวณจากค่าที่คุณเลือก เทียบสมมติฐานทำเอง 5–10 นาทีต่อเคส ไม่ใช่ผลการใช้งานจริงหรือเวลาที่รับประกัน ค่าติดลบหมายถึงใช้เวลามากกว่าฐานเทียบ</p></div></section>;
}
